<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.financialapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinancialApp">
        <activity
            android:name=".view.FBQuestionPage"
            android:exported="true" />
        <activity
            android:name=".view.ChoicePage"
            android:exported="true" />
        <activity
            android:name=".view.InformationPage"
            android:exported="true" />
        <activity android:name=".view.MainActivity" />
        <activity android:name=".view.QuizResult" />
        <activity android:name=".view.MCQuestionPage" />
        <activity android:name=".view.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>